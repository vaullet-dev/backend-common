<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.vaullet</groupId>
    <artifactId>common-parent</artifactId>
    <version>0.0.0-DEV</version>
  </parent>

  <artifactId>common-security</artifactId>
  <name>Vaullet Backend Common :: Security</name>
  <description>Stateless Keycloak JWT resource-server posture, per ADR-006 and ADR-008</description>

  <!--
    Separate from common-web on purpose. The two answer different questions — "what does
    an error look like" and "who may call this" — and a service can genuinely want the first
    without the second (an internal tool behind the mesh, a fixture app in a test suite). Keeping
    them apart also means spring-security is a transitive dependency of exactly the services that
    asked for it.

    Depends on web rather than the reverse: SecurityApiExceptionHandler extends the web module's
    handler to add the one @ExceptionHandler that needs a Spring Security type on the classpath.
  -->
  <dependencies>
    <dependency>
      <groupId>dev.vaullet</groupId>
      <artifactId>common-web</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security-oauth2-resource-server</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security-oauth2-resource-server-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
